import { PriceAboveZeroDirective } from './price-above-zero.directive';

describe('PriceAboveZeroDirective', () => {
  it('should create an instance', () => {
    const directive = new PriceAboveZeroDirective();
    expect(directive).toBeTruthy();
  });
});
