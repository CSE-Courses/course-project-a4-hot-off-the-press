import { QualitydirDirective } from './qualitydir.directive';

describe('QualitydirDirective', () => {
  it('should create an instance', () => {
    const directive = new QualitydirDirective();
    expect(directive).toBeTruthy();
  });
});
