import { SpecCharDirective } from './spec-char.directive';

describe('SpecCharDirective', () => {
  it('should create an instance', () => {
    const directive = new SpecCharDirective();
    expect(directive).toBeTruthy();
  });
});
